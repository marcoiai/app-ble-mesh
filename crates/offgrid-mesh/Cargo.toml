[package]
name = "offgrid-mesh"
version = "0.1.0"
edition = "2021"
description = "Transport-agnostic off-grid mesh protocol primitives"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
